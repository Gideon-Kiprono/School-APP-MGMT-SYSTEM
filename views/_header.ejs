<header>
  <h1>SAMS</h1>
  <nav>
    <a href="/">Home</a>

    <% if(locals.user && locals.user.role === "principal"){%>
      <a href="/permissions"> Permissions</a>
      <a href="/attendance"> Attendance</a>
      <% } %>
      <% if(locals.user && locals.user.role === "classteacher"){%>
      <a href="/Studentpermissions"> Permissions</a>
      <% } %>


      <% if(locals.user && locals.user.role === "security"){%>
      <a href="/attendance"> Attendance</a>
      <% } %>
    <a href="/newStudent">+ New student</a>
    <a href="/newStaff">+ New Staff</a>
    <a href="/news/sports/epl-transfers">Blog</a>
    <% if(locals.user){%>
      <a href="/logout">Log Out</a>
      <% }else{%>
    <a href="/login">Sign In</a>
    <% } %>
  </nav>
</header>

<!-- Layouts -->
<!-- SSR , CSR -->